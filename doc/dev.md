# Dev

## Build

1 - install Dotnet SDK (see ./vagrant/provision.sh for setup)
2 - clone project.

    cd ./src/Lastfm_dl

Build

    dotnet build

Run basic version print command

    dotnet run -- -v